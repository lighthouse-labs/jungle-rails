<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>
  <% if cart.size != 0 %>
  <%= render 'carts/cart' %>
  <% else %>
  <h2> <%= link_to 'Your cart is currently empty. Keep shopping 🐒', :root %> </h2>
  <% end %>

</section>