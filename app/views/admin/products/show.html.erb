<%# TODO: Fix style / HTML - KV %>

<h1><%= @product.name %></h1>

<p> PRODUCT DETAILS HERE (LATER) </p>

<div class="row">
  <h2>
    Images
    (<%= link_to "Add Image", "/admin/products/#{@product.id}/new" %>)
  </h2>

  <% @product.images.each do |image| %>
    <%= image_tag image.file.tiny.url %>
  <% end %>
</div>


